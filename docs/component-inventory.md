# Component Inventory

This project does not have a dedicated component library or a `components` directory.

The main user interface is encapsulated within a single React component:

- **`App.tsx`**: This is the root component of the application and contains all the UI elements for the game, including the game board, score display, and controls.
